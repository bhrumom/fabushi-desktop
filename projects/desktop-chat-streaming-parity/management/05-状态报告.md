# 状态报告

## 2026-09-20 — intake/root cause
Observed evidence matches five failures. Current code confirms: legacy `chat.delta` seals the prior streaming text before every delta (creating one text part per token); normal send creates a separate thinking DisplayMessage while runtime events create an AssistantTurn; BotConversationView uses `height:100%` above a sibling composer; account Bot refresh waits behind self-hosted bootstrap; final reconciliation relies on exact text equality/prefixes.

Next: implement T01-T04 and run focused CI.


## 2026-09-20 — implementation ready for CI
Implemented T01-T04 on the feature branch. PR #4 placed the dedicated hosted CI workflow on canonical main at `ca9cfc36f5c9a46a415d1f913d711803fb45b8ca`, so the next feature-head update can be tested by GitHub Actions before merge.
