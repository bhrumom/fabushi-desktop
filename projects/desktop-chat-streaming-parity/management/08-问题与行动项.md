# 问题与行动项

No user clarification is required for the reported defects.

Open actions:
- validate operation-id adoption when runtime events arrive before the chat.send promise resolves;
- prove the exact head in GitHub Actions;
- merge through protected main only after required checks pass.
