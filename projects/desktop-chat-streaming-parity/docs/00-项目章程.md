# 项目章程

问题：桌面 Agent 会话存在重复回复、流式碎片化、首 token/状态反馈慢、Mini App Bot 延迟出现和输入框布局不稳定。

目标：把一次发送建模为“用户本地即时消息 + 单一 assistant turn”，让 token 在同一正文中连续增长，并让输入框始终可操作。

主要用户：Fabushi 桌面用户。约束：保留现有 Electron + Rust Mahayana 架构；不以 WebView 套第三方网页替代本地 Agent runtime。

成功定义：见 02 与 19。
