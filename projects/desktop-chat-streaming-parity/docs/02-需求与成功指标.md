# 需求与成功指标

- R1: send click paints user row immediately and a single assistant in-progress surface without waiting for Host acceptance.
- R2: character-level `chat.delta` events coalesce into one streaming text part; no one-character-per-line rendering.
- R3: final `chat.message` / completion must reconcile into the same turn and must not duplicate already streamed text.
- R4: no separate normal-path “thinking” row plus assistant-turn row for one operation.
- R5: composer stays visible/in viewport while transcript grows or streams.
- R6: account/Mini App Bot membership hydration starts independently of slower self-hosted sync.
- R7: existing approval/error exceptional surfaces remain functional.

Success evidence: automated reducer/UI regression checks plus CI on the PR.
