# 发布迁移与回滚

No data migration. Changes are renderer/projection compatible.

Rollout: merge through protected main after CI. Rollback trigger: duplicated/missing assistant content, inability to send, hidden composer, or broken Bot identity. Rollback by reverting the project PR; Rust persisted data format is unchanged.
